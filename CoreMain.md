# CoreMain — 專案中心思想（North Star）

> 本檔由 PM 在專案最開始、與使用者確認後建立，是整個專案的「中心思想」。
> 之後所有功能開發、測試、驗收都以本檔為中心：圍繞主題、維持簡單順手不複雜。

## 一句話專案主題（Theme）
一個**完全離線的桌面工具**，讓資安分析師匯入兩份 Nessus 弱點掃描 CSV，**以 IP 為主 Key 快速比對差異（新增 / 修復 / 持續）**，並用 EPSS 與 VPR 兩種評分即時看出**哪些主機的漏洞要優先處理**，最後產出報告。

## 核心價值（North Star）
- 使用者用它最想得到的那一件事：**「這次掃描比上次，哪些主機的高風險漏洞變多了、該先修哪一台？」** 一眼看懂。
- 成功長什麼樣（一句話）：拖入兩份 CSV → 3 秒內看到差異總覽、優先主機清單與四象限圖 → 一鍵匯出報告。

## 範圍主軸（圍繞主題）
- 圍繞主題會做：CSV 匯入與欄位驗證、以 IP 為 Key 的 diff、快速篩選/排序、EPSS×VPR 風險圖表、漏洞總數統計、執行 Log、報表匯出。
- 與主題無關、明確不做（防 scope creep）：帳號登入、雲端同步、掃描排程、直接連 Nessus API、任何資料落地/歷史保存（依需求「不記憶」）、線上更新。

## 設計原則（所有功能一律遵守）
- **簡單**：主流程只有「匯入兩份 CSV → 看結果 → 匯出」三步；不看說明就會用。
- **順手**：拖放即匯入、分頁式導覽、篩選即時反應、常用動作路徑最短。
- **不複雜**：每個分頁只解決一件事；零外部相依、自繪圖表，不引入重框架。
- **取捨準則**：「功能更強」與「更簡單順手 / 低記憶體」衝突時，預設選後者。

## 架構決策（與使用者確認）
- **形式**：Electron 桌面 App（使用者選定）。
- **關鍵取捨（資安分析師註記）**：Electron 記憶體佔用天生較高，與「不要大量增加記憶體」有張力。
  對策：**核心邏輯放在精簡的純前端 renderer（vanilla JS + 自繪 SVG，零 npm 執行期相依）**，
  Electron 僅作為安全離線外殼（單一視窗、無網路、關窗即釋放）。此 renderer 亦可直接用瀏覽器離線開啟，
  作為零安裝備援。
- **不記憶原則**：不使用 localStorage / sessionStorage / IndexedDB / 任何檔案快取；所有資料僅存於 JS 記憶體，
  關閉視窗即由 GC 釋放；另提供「清除資料」按鈕主動釋放參考。
- **離線與 Electron 安全基線**：`contextIsolation:true`、`nodeIntegration:false`、`sandbox:true`、
  嚴格 CSP（`default-src 'self'`）、封鎖對外導覽與開新視窗、preload 只用 contextBridge 暴露最小且經驗證的檔案儲存 API。

## 主題對照檢核（每加一個功能前先自問）
- [ ] 這個功能是在服務「兩份 Nessus 掃描比對 + 優先處理判斷」嗎？（否 → 不做）
- [ ] 有沒有更簡單、更順手、更省記憶體的做法達成同樣目的？
- [ ] 它會不會讓整體變複雜、偏離主軸、或違反「不記憶 / 離線」原則？
