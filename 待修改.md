# 待修改（Secure SDLC 計畫）

## 目標
建立一個完全離線的 Electron 桌面工具「Nessus Diff」，匯入兩份 Nessus CSV，以 IP 為主 Key 顯示 diff，
提供篩選/排序、EPSS×VPR 風險圖表、漏洞統計、執行 Log 與報表匯出；關閉即釋放記憶體、不做任何持久化。

## 範圍與假設
- 範圍：CSV 匯入/驗證、IP 為 Key 的 diff、篩選排序、圖表、統計、Log、報表匯出。不做：登入、雲端、掃描、API 串接、資料落地。
- 假設：輸入為 Tenable Nessus 匯出的 `.csv`（UTF-8，含 BOM 亦可）；欄位順序不固定，以表頭名稱（含別名、忽略大小寫）對應。
- 技術棧 / 環境：Electron + 純前端 renderer（vanilla JS、自繪 SVG）；零 npm 執行期相依；Node 用於 Electron 主行程與檔案儲存 IPC。
- 測試環境存取：本機以 `npm start` 執行；renderer 亦可用瀏覽器直接開 `renderer/index.html` 驗證核心邏輯（憑證不適用，無外連）。

## 上線標準（Exit Criteria，三維度合一）
- 功能：需求 9 項全數覆蓋且可操作；缺欄位有明確提示與範例；大資料量（≥ 5 萬列）不當機、可用。
- 資安：Electron 安全基線全開（contextIsolation/sandbox/nodeIntegration off/CSP/封鎖外連）；顯示層無 XSS（一律 textContent/escape）；CSV 匯出防公式注入；無硬編碼密鑰；無執行期外部相依。
- 品質：核心解析/diff/評分邏輯以測試案例驗證通過；正常/邊界/異常路徑皆處理；無 Critical/Major 缺陷。

## 〔PM〕評估與設計
- [x] 確認專案主題、建立 `CoreMain.md`
- [x] 需求釐清與可行性評估（9 項需求對照主題）
- [x] 研究 Nessus 匯出 CSV 欄位（必要/選用、別名、缺欄位處理）
- [x] 架構設計（Electron 安全外殼 + 精簡純前端 renderer）
- [x] 訂出 Exit Criteria

## 〔DevSecOps〕編寫與資安確認
- [x] Electron 主行程（安全基線）+ preload（contextBridge 最小 API）
- [x] CSV 解析器（RFC4180、引號/換行、BOM、大檔分塊 + 進度）
- [x] 欄位對應與驗證（必要欄位缺漏提示 + 範例下載）
- [x] Diff 引擎（IP 為主 Key；finding key = pluginId+port+protocol；新增/修復/持續/變更）
- [x] 篩選 + 排序（即時、debounce、虛擬捲動大表）
- [x] 圖表（嚴重度分佈對比、diff 總覽、Top 風險主機、EPSS×VPR 四象限）
- [x] 統計（總數、各嚴重度、主機數、diff 統計）
- [x] 執行 Log（分級、window.onerror 攔截、匯出）
- [x] 報表匯出（HTML 報告含圖表 + CSV 匯出；可套用目前篩選）
- [x] 資安自審：Electron 安全基線、XSS(escape)、CSV 公式注入防護、路徑/IPC 驗證
- [ ] 待修補弱點：（見資安自審結論，無 Critical/High 未解）

## 〔QA〕品質測試
- [x] TC-001 正常匯入兩份完整欄位 CSV → diff 正確
- [x] TC-002 缺選用欄位（無 VPR/EPSS）→ 圖表優雅降級、不崩潰
- [x] TC-003 缺必要欄位 → 明確錯誤 + 範例提示
- [x] TC-004 含引號/逗號/換行的欄位值 → 解析正確
- [x] TC-005 diff：新增/修復/持續/主機新增移除 分類正確
- [x] TC-006 CSV 公式注入字串（=,+,-,@）匯出時前置防護
- [x] TC-007 大資料量（合成 5 萬列）→ 匯入/篩選/圖表可用
- [ ] 待修缺陷：（見 lessons.md）

## 〔CI/CD〕交付與部署
- [ ] 驗證目標：GitHub（本任務推送分支）；地端以 npm start
- [ ] 後續可補 GitHub Actions（lint/測試）— 非本輪必要

## 關卡狀態（由編排器維護）
- [x] G1 PM→Dev：設計確認、Exit Criteria 訂定
- [x] G2 Dev→Sec：功能完成、單元自測通過
- [x] G3 Sec→QA：資安 Exit Criteria 達標
- [x] G4 QA→結案：品質 Exit Criteria 達標
- [ ] G5 結案→CI：（後續）
- [ ] G6 CI→CD：（後續）

## 已知風險
- Electron 記憶體佔用高於純瀏覽器方案（已用精簡 renderer + 單視窗 + 清除按鈕緩解；README 記載）。
- 超大檔（數十萬列）於單行程解析仍可能吃記憶體；以分塊解析 + 虛擬捲動 + Top-N 圖表限制緩解。
- Nessus 不同版本欄位命名差異；以別名對應 + 缺欄位降級處理。
