# lessons — Secure SDLC 輪結與教訓

## [2026-09-05] 輪結 Round 11 — 四象限 CVSS 軸留頭部空間、修虛擬表捲不到底（v1.7.1）

- 需求：(1) 四象限 CVSS 軸調到 11 避免資料點跑出圖表外；(2) 差異比對表捲不到最後資料。
- Bug 根因（捲不到底）：虛擬捲動的 paint 是 renderVirtual 內的 closure，捕捉了當次的 `total`；scroll 事件只綁定一次（第一次渲染，常在只載入基準那份、或分頁隱藏 clientHeight=0 時），之後資料量變大但 scroll handler 仍用舊的較小 total → 捲到底時 `start > end` → 一列都不渲染。修法：把 paint 抽成模組層級函式，每次即時讀 `S.filtered.length` / `S.detailFiltered.length`；並在切到差異分頁時重繪（修首次隱藏渲染高度為 0）。詳細表同修。
- Dev（軸）：AXES 加 `ticks` 明確刻度陣列；CVSS 軸 max=11、刻度仍標到 10，象限背景改由 Y(max) 起算，頂部點與「⚠優先處理」標籤都有空間。
- QA：核心 78 項全過；端對端驗證捲到底可見 20 列且最後一列為真實資料、詳細表同、CVSS 刻度到 10 域到 11、0 錯誤。
- 教訓：**虛擬捲動的 paint 絕不可捕捉當次的 total／資料長度**——scroll handler 綁定一次卻長期存活，必須每次即時讀取資料源，否則資料量變動後捲動渲染會錯位或空白。分頁隱藏時首次渲染的 clientHeight=0，切回該分頁要重繪。

## [2026-09-05] 輪結 Round 10 — 範例擴至300筆、修圖表重疊、四象限游標提示、總覽精簡、全域資料來源（v1.7.0）

- 需求：(1) 範例資料增至 300 筆；(2) 檢查圖表與說明是否重疊；(3) 四象限游標指到點要顯示 CVE/IP；(4) 總覽優先表資料置中、移除「優先分數」欄；(5) 移除總覽「最高優先主機」KPI；(6) 風險明細的基準/當前切換改為全域，只有差異比對維持對比。
- Dev：(1) 以真實 plugin/CVE 池生成 baseline 298 / current 300（40+ 主機、真實嚴重度分佈與 diff、含 Solution）；(2) chartSeverity/chartTotalsCompare 增大上邊距、圖例下移，避免與長條數值重疊；四象限「⚠優先處理」標籤移到上緣。(3) 新增跟隨游標的浮動提示（.chart-tip）+ 透明較大命中圈，四象限顯示 主機/CVE/名稱/座標值。(4) renderPriorityTable 移除 score 欄、table.centered 置中。(5) renderKPI 移除最高優先主機卡。(6) 新增全域 S.viewSource + topbar 切換鈕；風險圖表單一來源圖（四象限/熱力圖/Top 主機）與風險明細改吃全域來源，比較圖與差異比對維持對比；每次匯入預設「當前」。
- QA：核心 78 項全過（[4] 改用內建 fixture 不依賴範例檔、[5b] 驗範例可解析且 ≥250 筆）；端對端驗證六項全部、四象限 hover 顯示 CVE、0 錯誤。
- 教訓：單元測試不要依賴會變動的示範資料，改用內嵌 fixture。SVG 小圓點原生 <title> 使用者不易觸發/看見 → 加透明大命中圈 + 跟隨游標的浮動 div 更可靠。移除欄位/卡片時記得同步移除其排序/計算殘留（bar-cell、maxScore）。

## [2026-09-05] 輪結 Round 9 — 熱力圖 VPR/EPSS 加總切換、Top 主機改嚴重度堆疊、圖表標示來源（v1.6.0）

- 需求：(1) 熱力圖改為單純 Σ VPR 或 Σ EPSS，面板內切換；(2) Top 風險主機改為依主機弱點總數、以嚴重度堆疊成一條長條（Info 藍/Low 綠/Medium 黃/High 紅/Critical 橘）；(3) 每張圖標示資料來源（基準/當前/對比）。
- 前情：使用者問優先分數公式出處 → 誠實說明那是我自訂 heuristic（非官方標準），本輪即改為更直觀的「單純加總」與「純數量堆疊」，不再依賴自訂複合分數。
- Dev：core `computeHostPriority` 每台主機加算 `sumVpr`/`sumEpss` 與完整 `sev{5級}`。chartHeatmap 依 metric 取 sumVpr/sumEpss、依該值排序著色；面板加 Σ VPR/Σ EPSS 切換。chartTopHosts 改為依 count 排序的水平堆疊長條（自訂色：Info#4f8cff/Low#2dd4a7/Medium#ffd23f/High#ff4d6d/Critical#ff8c42）+ 圖例。renderCharts 設定各圖 `#src-*` 來源徽章（單一來源=當前/基準檔名；totals/severity=基準 vs 當前對比）。
- QA：核心 77 項全過（新增 sumVpr/sumEpss/sev 驗證）；端對端驗證熱力圖切換（Σ VPR 18.9↔Σ EPSS 1.88）、Top 主機堆疊+圖例、四來源徽章、0 錯誤。
- 註記：使用者指定 High=紅、Critical=橘（與 app 其他處 Critical=紅 相反），已照指定實作並於回覆提醒可再翻轉。
- 教訓：當自訂複合指標說不清出處時，回歸「使用者直覺可懂的原始量（加總/數量）」往往更好；不同圖表若要各自配色，用區域色表而非全域 RISK_COLORS，避免相互牽動。

## [2026-09-05] 輪結 Round 8 — 主畫面精簡、熱力圖改版、新增風險明細分頁（v1.5.0）

- 需求：移除主畫面「離線·不記憶」說明與「清除資料」按鈕（改為關閉自動釋放）；嚴重度圖每根直條顯示數字；修熱力圖「沒顯示資訊」；移除「優先主機」分頁；Top 風險主機補計算說明；新增「風險明細」分頁（每 CVE 一列的矩陣，含處理方式，可選 CSV + 篩選）。
- Dev：(1) topbar 移除 mem-hint / btn-clear（記憶體本就靠 Electron 關窗 quit 由 OS 回收，clearAll 一併刪除）。(2) chartSeverity 兩根長條都標數字（含 0）。(3) 熱力圖根因＝少量主機時 22px 小方塊擠左上看似空白 → 改為「有標籤色塊磚」(IP+分數，min 104px grid，依亮度決定字色)。(4) 移除整個優先主機分頁（主機聚合虛擬表/網段彙總/明細抽屜）與相關 state/binding；heatmap 點擊不再導頁。(5) Top 風險主機面板加計算方式說明。(6) 新增風險明細：core 加回 solution/synopsis；每個 plugin 依 CVE 展開成多列；虛擬捲動表 + 來源(基準/當前)切換 + 搜尋/嚴重度/VPR/EPSS 篩選 + 點列展開摘要與 Solution。
- QA：核心 72 項全過；端對端驗證 mem-hint/btn-clear 移除、優先主機分頁移除、嚴重度圖 10 標籤、熱力圖標籤磚、風險明細每 CVE 一列+來源切換+篩選+展開 Solution、0 錯誤。
- 教訓：色塊熱力圖在「資料點少」時會顯得空白無意義——加上標籤(IP+值)讓它在少量與大量主機都成立。移除大功能時務必連帶清掉其 state/事件綁定/其他圖表對它的呼叫(如 heatmap 導頁)，避免殘留引用。

## [2026-09-05] 輪結 Round 7 — 風險圖表 IP 多選篩選器（v1.4.0）

- 需求：風險圖表加 IP 篩選、可多選主機、圖表即時更新、IP 依降序排列。
- Dev：新增可搜尋的 IP 多選下拉（checkbox 清單），套用到該分頁所有圖表（四象限/總數比較/熱力圖/嚴重度/Top 主機）。實作：`renderCharts` 依所選主機集合即時重算 recs/rows/stats/priority 再傳入各圖；`chartQuadrant` 改為可接收 cfg.recs/cfg.rows。排序預設「風險分數降序」，另附「IP 降序」切換（IPv4 轉數值比較）。全不選→圖表顯示提示；預設全選。變更以 150ms debounce 即時重繪。
- QA：核心 72 項全過；端對端（300 IP）驗證清單風險降序、IP 降序、勾 3 台四象限 800→102 點即時更新、全不選提示、按鈕計數正確、0 錯誤。
- 教訓：多選篩選套用到「多張共用資料的圖表」時，正解是在單一入口（renderCharts）算一次篩選後資料集、再分發給各圖，而非每張圖各自讀全域——避免重複與不一致。全選/空集合的語意要明確：sel===total=全部、sel===0=無（顯示提示），按鈕文案需分辨兩者。

## [2026-09-04] 輪結 Round 6 — 四象限資料點 hover 顯示 CVE（v1.3.1）

- 需求：四象限每個資料點,游標停留時顯示 CVE 名稱。
- Dev：建點時帶入 `r.cve`,原生 SVG `<title>` 提示加一行 `CVE: ...`（弱點模式）;主機模式因一點含多 CVE,不顯示單一 CVE。零額外相依、離線可用。
- QA：端對端讀取 `<title>` 內容驗證,弱點模式 5/5 點含 CVE、主機模式正確省略、0 錯誤。

## [2026-09-04] 輪結 Round 5 — 新增「漏洞總數與各嚴重度比較」長條圖（v1.3.0）

- 需求：風險圖表加一個條狀圖,比較兩份 CSV 的漏洞總數與各風險總數。
- Dev：新增 `chartTotalsCompare`——分組長條,類別=[總數,Critical,High,Medium,Low,Info],每組左=基準(半透明)、右=當前(實色),每根標數值,下方標增減量(▲/▼/＝);單份模式只顯示當前。加入風險圖表分頁,並納入 HTML 報表的嚴重度區塊。
- QA：核心 72 項單元測試維持全過;端對端渲染驗證 6 類別、12 長條、增減量正確(總數＝、Critical ▲+2、High ▲+1、Medium ▼-3)、0 錯誤。
- 教訓：總數與各嚴重度同圖時,總數量級常遠大於個別嚴重度,務必在每根長條標上數值,即使短柱也能讀出精確數字(避免只靠柱高判讀)。

## [2026-09-04] 輪結 Round 4 — 四象限改 Y=CVSS v2.0、X 可切 EPSS/VPR（v1.2.0）

- 需求：使用者要四象限縱軸固定 CVSS v2.0、橫軸切換 EPSS 或 VPR（選「一張圖 + 橫軸切換」）。
- Dev：(1) core.js 每筆記錄改保留獨立 `cvss2` / `cvss3`（原本只存合併值）；(2) `chartQuadrant` 重構為軸可設定（cfg.xKey）、Y 固定 CVSS v2.0、整份缺 v2.0 時降級用 v3.0 並於圖上註記；(3) 新增「橫軸 = EPSS / VPR」切換鈕，門檻輸入範圍隨橫軸自動調整（EPSS 0–1 / VPR 0–10）；(4) 缺 x/y 值的點不繪並標示筆數。
- QA：核心 72 項單元測試全過（新增 CVSS v2/v3 分開保留 5 項）；端對端驗證兩種橫軸下 Y 皆為 CVSS v2.0、切換正確、門檻範圍同步、所有點在 viewBox 內、0 錯誤。
- 教訓：座標軸語意（哪個分數在哪軸）要讓使用者可切換而非寫死；CVSS 有 v2/v3 兩版，正規化時就該分開保留、並設計「缺某版自動降級」路徑。

## [2026-09-04] 輪結 Round 3 — 修正四象限座標溢出（v1.1.1）

- 現況：QA 提問「風險圖表資料會不會跑出畫面外導致辨識困難」。查核四象限 `chartQuadrant` 座標映射，確認**有真實缺陷**。
- 根因：(1) EPSS 若匯出成百分比（`97%`→`num()` 去 % 變 97），`X(97)` 飛出 viewBox；SVG 預設 `overflow:hidden` 會**靜默裁掉**該點（最嚴重弱點反而消失），且 `vpr/10×97` 也汙染優先分數。(2) VPR>10 或負值使 Y 溢出。(3) 門檻輸入超界使參考線/象限底色跑出格線。
- 修法（兩層）：(a) 資料源頭正規化 `normEpss`（>1 視為百分比 /100、夾 [0,1]）、`normVpr`（夾 [0,10]）、CVSS 夾 [0,10]——同時修正被汙染的優先分數；(b) `chartQuadrant` 內 X/Y 映射與門檻一律夾範圍，作為第二層防禦。
- QA：新增 12 項正規化/夾範圍單元測試（共 67 項全過）；端對端用髒 CSV（EPSS=97%/88/250、VPR=99/-3）＋超界門檻（2/15）驗證，所有點與參考線 100% 落在 viewBox 內、0 錯誤。
- 教訓：**SVG 溢出不是「跑出畫面」而是「被裁掉消失」，比看得到還危險**。凡有定義域的量（EPSS 0–1、VPR/CVSS 0–10）務必在資料源頭正規化＋繪圖端夾範圍雙重保險；EPSS 尤其要防「百分比 vs 機率」兩種尺度。

## [2026-09-04] 輪結 Round 2 — 大量主機 / 大資料量畫面優化（v1.1.0）

- 現況：使用者提出「300 IP」與「1 萬筆」兩種規模的畫面優化。四項優化全數實作並驗證。
- Dev：新增（a）優先主機分頁改為**主機聚合虛擬捲動**清單＋點列展開明細抽屜；（b）**網段 /24 彙總**（core: `computeSubnetAggregation`）；（c）**主機風險熱力圖**（DOM 色塊、限 600 格）；（d）四象限「一點=弱點/主機」切換；（e）差異表**搜尋預建索引** `_hay`＋**預設收斂**（隱藏 Info、只看需行動）。
- QA：核心 55 項單元測試全過（含網段彙總 7 項）；合成 300 IP × 兩份各 1 萬筆（共 2 萬筆）端對端 0 錯誤——匯入＋比對 ~1.3s、差異表 2 萬列僅渲染 ~4 列、主機表 300 台僅渲染 ~17 列、網段 5 段、熱力圖 300 格、四象限 800→297 點。
- 過關狀態：G2 ✓ / G3 ✓（顯示層仍全 textContent/escape，新增 DOM 皆以節點建立）/ G4 ✓

### 教訓 / 準則（Round 2）
- 情境：資料規模變大時要先分辨問題性質。
  準則：**「多主機」是認知/導覽問題（用聚合、彙總、熱力圖收斂），「多列」是渲染/運算問題（用虛擬捲動、預建索引、資料點上限）**——兩者解法不同，別混為一談。
- 情境：虛擬捲動清單要支援「展開明細」。
  準則：展開用**固定高度列 + 獨立明細抽屜**（點選觸發、於清單下方顯示該主機明細），避免可變列高破壞虛擬捲動的位移計算。
- 情境：散佈圖在大資料量會 overplotting。
  準則：提供**聚合切換**（一點=一個實體 vs 一點=一群），讓同一張圖在不同規模都可讀。
- 情境：測試資料若欄位含逗號未加引號，會造成欄位位移。
  準則：產生 CSV 測試資料時，含逗號/引號的欄位務必依 RFC4180 正確跳脫；同時這也驗證了 app 對畸形列不崩潰（穩健性）。

## [2026-09-04] 輪結 Round 1 — Nessus Diff 建置（v1.0.0）

- 現況：已過 G1~G4；G5/G6（CI/CD 自動化）留待後續（本輪以本機驗證與 push 分支交付）。下一步 = 使用者驗收，需要時補 GitHub Actions。
- PM：主題＝「兩份 Nessus 掃描以 IP 為 Key 比對 + EPSS/VPR 優先處理」，寫入 `CoreMain.md`。使用者選 Electron；資安分析師註記「Electron 記憶體張力」，對策＝精簡純前端 renderer 當核心、Electron 只當離線安全外殼。Exit Criteria 三維度定義完成。
- Dev/Sec：實作 Electron 安全外殼 + 純前端 renderer；弱點 Critical 0 / High 0（自審：Electron 安全基線全開、CSP `connect-src none`、XSS 全用 textContent/escapeXml、CSV 防公式注入、IPC 輸入驗證、無 prototype pollution 因 key 皆固定集合或以 Map 承載）。執行期零外部相依。
- QA：核心邏輯 48 項單元測試全過；Playwright 端對端（載入→匯入兩份→總覽/差異/圖表/優先/Log/清除）0 console 錯誤；報表產出驗證通過；5 萬×2 列效能 ~0.5s 解析、~0.45s 比對、heap ~130MB。
- 過關狀態：G1 ✓ / G2 ✓ / G3 ✓ / G4 ✓ / G5 － / G6 －

### 教訓 / 準則
- 情境：需求含「桌面 App」又要「低記憶體」時。
  準則：把核心做成**框架無關的純前端 + 自繪 SVG**，桌面殼（Electron）只負責安全與檔案 IO；核心同時能瀏覽器直開，兼顧體驗、記憶體與可攜性。
- 情境：要對「含 DOM 的前端邏輯」寫可自動化測試時。
  準則：把**純邏輯抽成無 DOM 的 core 模組**（UMD 匯出），瀏覽器與 Node 測試共用同一份，測試才真正涵蓋出貨程式碼。
- 情境：優先分數/圖表依賴 EPSS、VPR，但 Nessus 匯出未必都有。
  準則：一律設計**降級路徑**（缺 EPSS 改嚴重度+VPR 排序、缺 Risk 由 CVSS 推導），並於 Log 明確警示，不讓缺欄位造成崩潰。
- 情境：Electron 封鎖開新視窗會讓「報表預覽」失效。
  準則：`setWindowOpenHandler` 只放行自產、已 escape 的 `about:blank` 內容視窗，外部網址導向系統瀏覽器，兼顧安全與功能。
- 情境：大資料量表格。
  準則：**虛擬捲動**（只渲染可見列）+ 圖表資料點上限 + 只存必要欄位，是同時顧到「可用」與「低記憶體」的關鍵。
- 情境：任何要顯示或匯出使用者提供的 CSV 內容。
  準則：顯示一律 textContent、報表一律 escapeXml、CSV 匯出一律防公式注入（`= + - @` 前置 `'`）——三者缺一都可能被弱點報告內容反殺。

## [2026-09-05] 強化輪 v1.8 — 上線前硬化

- 現況：針對「離線資安工具」的正式上線標準，收斂五個面向：資料完整性(A)、正式報告欄位(B)、大檔串流(C)、離線邊界(D)、完整 QA(E)。核心測試由 78 擴至 169 項，另加真實瀏覽器 E2E 10 項；線上 Demo 同步更新。
- A 資料完整性：
  - 數值非法值（EPSS/CVSS/VPR 超界或非數值）改為**回報 issue 並存 null，不再夾到最大值**——夾值會製造「看似正常」的假資料，對資安判讀是更危險的錯。`parseScoreRaw`/`parseEpssRaw` 回 `{value, issue}`，`normalize` 收集 issues（上限 300）與計數。
  - EPSS 三格式（`0.97`／`97`／`97%`）以「帶 % 或 >1 就除以 100」統一；換算後仍 <0 或 >1 才算非法。
  - 重複列（相同 Host/Plugin/Port/Protocol）以 Set 偵測、**只標記計數不刪除**（重複本身是掃描品質訊號，刪了就看不到）。
  - 變更分類 `classifyChange` 產生 `changeTypes`（風險/分數/CVE/名稱/修補），差異表可顯示變更了「什麼」。
  - 新增 CVE 統計改以「當前所有 CVE 集合 − 基準 CVE 集合」，**涵蓋持續弱點內容變更後才出現的 CVE**（舊版只算 added 列會漏）。
- B 報告欄位：normalize 擴充 Description/Plugin Output/See Also/資產(DNS/OS/MAC)/處理狀態/例外原因/負責人/備註；`extractScanTime` 由 Scan Information plugin(19506) 的 Plugin Output 擷取掃描時間；報告保留來源檔名 + 掃描時間 + 產製時間 + 負責人/備註，可追溯。
- C 大檔：文件曾宣稱分塊、實作卻整檔讀入——**文件與實作不一致本身就是缺陷**。改 `createStreamParser`（可續傳、處理跨 chunk 的 `""` 與 CRLF）+ `file.stream()` 逐塊讀取，加進度、可取消、200MB 大小限制；實測 6 萬列 < 1s。
- D 離線邊界：**移除 `shell.openExternal`**（推翻 v1.0「外部網址導向系統瀏覽器」的設計）——真正的離線工具連「交給系統瀏覽器」都不該做，否則仍是一條對外資料路徑。以 `session.webRequest.onBeforeRequest` 攔截所有非本機請求、`setPermissionRequestHandler(false)`、封鎖 will-navigate/will-redirect，縱深防禦。
- E QA：新增單檔/空檔/缺欄位/非法數值/重複/主機格式、報告與畫面統計一致、大量效能、XSS/CSV 公式注入/CSP 靜態不變式；真實 Chromium E2E 驗證 XSS payload 不被執行、頁面主動 fetch 外連被 CSP 擋下。
- 過關狀態：G1 ✓ / G2 ✓ / G3 ✓ / G4 ✓ / G5 － / G6 －

### 教訓 / 準則
- 情境：數值欄位遇到超界或髒值。
  準則：**標記為缺值(null) + 記錄 issue，絕不夾到最大/最小值**。夾值把異常偽裝成正常，在資安脈絡下比缺值更危險；把「這格有問題」明確傳達給人判讀。
- 情境：偵測到重複資料。
  準則：**標記並呈現，不要自動去重**。重複往往是掃描設定或資產清單的品質訊號，靜默刪除等於湮滅證據。
- 情境：文件宣稱的能力（如「分塊解析」）與實作不符。
  準則：**視為缺陷立即修正**，不要讓 README 成為謊言；能力宣稱要能被測試證實（本輪以 6 萬列實測 + 串流/一次性結果一致性測試把關）。
- 情境：號稱「完全離線」的工具。
  準則：**連「交給系統瀏覽器開啟外部連結」都要移除**。離線的定義是「沒有任何對外資料路徑」，任何 openExternal/外連導覽都是破口；以請求攔截 + 導覽封鎖做縱深防禦，並以靜態不變式測試防止日後退化。
- 情境：安全設定（CSP、Electron flags、無 openExternal）容易在後續改動被悄悄破壞。
  準則：把安全不變式寫成**會失敗的測試**（掃 main.js/index.html 原始碼斷言），讓退化在 CI 就被擋下，而非上線後才發現。
- 情境：既有設計準則在新需求下可能過時（v1.0 曾主張外連導系統瀏覽器）。
  準則：**準則要能被推翻**；當上線標準提高（完全離線），舊的便利性設計就該讓位給更嚴格的安全邊界，並在 lessons 明確記錄「為何反轉」。
